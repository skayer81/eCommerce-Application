// import { BasketDataList } from './basketTypes';

// export function getTotalCost(data: Array<BasketDataList>): number {
//   const result = data.reduce((acc, item) => item.price * item.quantity + acc, 0);
//   return result;
// }
